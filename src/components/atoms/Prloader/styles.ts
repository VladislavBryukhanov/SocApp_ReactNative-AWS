import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  preloader: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  image: {
    maxHeight: 100,
    maxWidth: 100,
    justifyContent: 'center',
  }
})