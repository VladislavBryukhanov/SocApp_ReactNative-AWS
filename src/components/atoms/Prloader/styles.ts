import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  preloader: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  image: {
    maxHeight: 75,
    maxWidth: 75,
    justifyContent: 'center',
  }
})